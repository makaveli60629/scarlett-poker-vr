// Scarlett controls module stub (safe). Replace with your Scarlett 1 controls.js if desired.
export async function setupControls({scene, rig, camera, renderer, THREE, log}){
  log('[controls] stub loaded ✅');
  return null;
}
