// js/hand_teleport.js — SAFE STUB (FULL)
// Fixes: "Unexpected end of input" parse crash.
// You can replace this later with your real hand teleport.

export function init(ctx){
  ctx?.log?.("[hand_teleport] stub init");
  return true;
}

export function setup(ctx){
  ctx?.log?.("[hand_teleport] stub setup");
  return true;
}

export function start(ctx){
  ctx?.log?.("[hand_teleport] stub start");
  return true;
}
