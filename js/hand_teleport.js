// js/hand_teleport.js — SAFE STUB (prevents parse errors)
// Replace with your real implementation later.

export function init(ctx){ ctx?.log?.("[hand_teleport] stub init"); return true; }
export function setup(ctx){ ctx?.log?.("[hand_teleport] stub setup"); return true; }
export function start(ctx){ ctx?.log?.("[hand_teleport] stub start"); return true; }
