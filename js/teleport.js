// Scarlett teleport module stub (safe). Replace with your Scarlett 1 teleport.js if desired.
export async function setupTeleport({scene, rig, camera, renderer, THREE, log, controls}){
  log('[teleport] stub loaded ✅');
  return null;
}
